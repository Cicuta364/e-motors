<%= form_for(@data_sheet) do |f| %>
  <% if @data_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_sheet.errors.count, "error") %> prohibited this data_sheet from being saved:</h2>

      <ul>
      <% @data_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :hz %><br>
    <%= f.number_field :hz %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
